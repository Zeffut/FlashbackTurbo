package fr.zeffut.flashbackturbo.gui;

/**
 * Legacy H8 screen placeholder.
 *
 * <p>The 26.2 port disables H8 because Flashback now reports finalization
 * progress itself and Minecraft 26.2's render-state path made the old manual
 * render/swap approach brittle. Kept as a source-compatible placeholder so old
 * documentation references still resolve without loading a runtime mixin.
 */
public final class SavingExportScreen {
    private SavingExportScreen() {}
}
